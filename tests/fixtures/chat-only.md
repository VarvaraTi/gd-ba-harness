# Chat-only INITIAL

Input: Users need to save a payment method, reuse it, and remove it.

Expected controls:
- register the message as `CHAT-<run-id>`;
- confirm one bounded change and owner;
- ask only material questions within the configured budget;
- produce Draft requirements before explicit approval.
