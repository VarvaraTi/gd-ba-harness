# Read-only VALIDATE

Input: An existing approved requirement set with one untestable criterion and recorded baseline hashes.

Expected controls:
- use a fresh reviewer context;
- write only `Analysis/validation-report.md`;
- report the untestable criterion as a finding;
- leave requirement and handoff hashes unchanged;
- route semantic correction to UPDATE.
