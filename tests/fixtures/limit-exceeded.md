# Limit exceeded

Input: Six selected sources with the default maximum of five.

Expected controls:
- stop before drafting;
- name the exceeded source limit;
- propose a non-overlapping split;
- wait for the user to select one bounded scope.
